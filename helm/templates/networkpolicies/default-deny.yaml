{{- if .Values.networkPolicy.enabled }}

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: {{ .Values.networkPolicy.defaultDeny.name }}
  namespace: {{ .Values.namespace.name }}

spec:

  podSelector: {}

  policyTypes:
    - Ingress
    - Egress

{{- end }}